import { test } from '../fixtures/page-fixture';

test.describe('Shopping cart tests', () => {
  test('Can add two items to shopping cart ', async ({  }) => {  
    //ARRANGE
    
    //ACT
    
    //ASSERT
  });
});
